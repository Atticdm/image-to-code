# 🔄 Настройка автодеплоя на Railway

## Проблема

Railway не делает автоматический деплой после новых пушей в GitHub.

## Причина

Railway нужно настроить для автоматического деплоя из GitHub репозитория.

## Решение

### Вариант 1: Настроить GitHub Integration (рекомендуется)

1. Откройте Railway Dashboard: https://railway.app/project/ff479365-a5ed-45ff-9afc-e38d711e7fbc
2. Перейдите в **Settings → Source**
3. Убедитесь, что подключен GitHub репозиторий:
   - Должен быть указан: `Atticdm/image-to-code`
   - Branch: `main`
   - Auto Deploy: **Включен** ✅

4. Если репозиторий не подключен:
   - Нажмите "Connect GitHub"
   - Выберите репозиторий `Atticdm/image-to-code`
   - Выберите branch `main`
   - Включите "Auto Deploy"

### Вариант 2: Ручной деплой через Railway CLI

Если автодеплой не работает, можно деплоить вручную:

```bash
railway deploy
```

Или через MCP:
```python
mcp_Railway_deploy(workspacePath="/Users/attic/Image-to-code")
```

### Вариант 3: Проверить GitHub Webhook

1. Откройте GitHub репозиторий: https://github.com/Atticdm/image-to-code
2. Settings → Webhooks
3. Проверьте, есть ли webhook для Railway
4. Если нет - Railway должен создать его автоматически при подключении

## Проверка

После настройки:
1. Сделайте новый коммит и пуш
2. Railway должен автоматически начать деплой
3. Проверьте в Railway Dashboard → Deployments

## Текущий статус

- Последний коммит в git: `ebdcaf1`
- Последний деплой Railway: `d460647a` (коммит `c9eeafa`)
- Отставание: 5 коммитов не задеплоены

## Быстрое решение

Запустите деплой вручную:
```bash
railway deploy
```

Или через Railway Dashboard:
1. Откройте сервис
2. Нажмите "Deploy" → "Deploy Latest"

