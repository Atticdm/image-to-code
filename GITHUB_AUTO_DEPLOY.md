# 🔄 Настройка автодеплоя из GitHub на Railway

## Проблема

Railway сейчас делает деплой через CLI (`railway up`), поэтому не подхватывает автоматически пуш из GitHub.

## Решение: Подключить GitHub Integration

### Шаг 1: Откройте Railway Dashboard

1. Перейдите: https://railway.app/project/ff479365-a5ed-45ff-9afc-e38d711e7fbc
2. Выберите сервис `image-to-code`

### Шаг 2: Настройте GitHub Source

1. Перейдите в **Settings** → **Source**
2. Найдите секцию **"Connect GitHub"** или **"Source"**
3. Если репозиторий не подключен:
   - Нажмите **"Connect GitHub"** или **"Change Source"**
   - Выберите репозиторий: `Atticdm/image-to-code`
   - Выберите branch: `main`
   - Включите **"Auto Deploy"** ✅
4. Если репозиторий уже подключен, но Auto Deploy выключен:
   - Включите переключатель **"Auto Deploy"**

### Шаг 3: Проверьте настройки

После подключения GitHub:
- Railway автоматически создаст webhook в GitHub
- При каждом пуше в `main` будет запускаться новый деплой
- В логах деплоя будет указано: `via GitHub` вместо `via CLI`

### Шаг 4: Проверка

1. Сделайте тестовый коммит и пуш:
   ```bash
   git commit --allow-empty -m "Test auto-deploy"
   git push origin main
   ```

2. В Railway Dashboard → Deployments должен появиться новый деплой с пометкой `via GitHub`

## Альтернатива: Через Railway CLI (если Dashboard недоступен)

Если нужно настроить через CLI:

```bash
# Проверить текущий источник
railway status

# Подключить GitHub репозиторий
railway link --repo Atticdm/image-to-code

# Или через Railway Dashboard (рекомендуется)
```

## Важно

После подключения GitHub:
- ✅ Деплои будут автоматическими при каждом пуше
- ✅ В логах будет видно коммит и автора
- ✅ Можно будет откатываться к предыдущим версиям через GitHub коммиты

## Текущий статус

- **Проект ID**: `ff479365-a5ed-45ff-9afc-e38d711e7fbc`
- **Сервис**: `image-to-code`
- **Репозиторий**: `Atticdm/image-to-code`
- **Branch**: `main`

После настройки GitHub integration, все будущие пуши будут автоматически деплоиться! 🚀

