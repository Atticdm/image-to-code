# ✅ Чеклист настройки Railway

## Backend Сервис (`image-to-code/Back`)

### ✅ Переменные (должны быть настроены):
- [x] `OPENAI_API_KEY` - для GPT моделей
- [x] `ANTHROPIC_API_KEY` - для Claude моделей  
- [x] `GEMINI_API_KEY` - для Gemini моделей

### ⚠️ Проверьте:
- [ ] Root Directory установлен: `backend`
- [ ] Сервис успешно деплоится (проверьте Deployments)
- [ ] Backend URL доступен: `https://image-to-code-production.up.railway.app`

---

## Frontend Сервис (`image-to-code/Front`)

### ✅ Переменные (должны быть настроены):
- [x] `VITE_HTTP_BACKEND_URL` - URL бэкенда для HTTP запросов
- [x] `VITE_WS_BACKEND_URL` - URL бэкенда для WebSocket соединений
- [x] `VITE_IS_DEPLOYED` - флаг деплоя (должно быть `true`)

### ⚠️ Важно проверить значения:

**VITE_HTTP_BACKEND_URL** должен быть:
```
https://image-to-code-production.up.railway.app
```
(с `https://`, не `http://`)

**VITE_WS_BACKEND_URL** должен быть:
```
wss://image-to-code-production.up.railway.app
```
(с `wss://`, не `ws://`)

**VITE_IS_DEPLOYED** должно быть:
```
true
```
(строка `"true"`, не булево значение)

### ⚠️ Проверьте:
- [ ] Root Directory установлен: `frontend`
- [ ] Dockerfile Path: `Dockerfile.prod`
- [ ] Сервис успешно деплоится (проверьте Deployments)
- [ ] Frontend URL доступен и показывает интерфейс

---

## 🧪 Тестирование

После настройки всех переменных:

1. **Проверьте Backend:**
   - Откройте: `https://image-to-code-production.up.railway.app`
   - Должно показать: "Your backend is running correctly..."

2. **Проверьте Frontend:**
   - Откройте URL вашего Frontend сервиса
   - Должен показать красивый темный интерфейс "Image to Code"
   - Попробуйте загрузить скриншот и сгенерировать код

3. **Проверьте подключение:**
   - В консоли браузера (F12) не должно быть ошибок WebSocket
   - При генерации кода должно быть подключение к бэкенду

---

## 🐛 Если что-то не работает:

1. **Проверьте логи:**
   ```bash
   railway logs --service image-to-code
   railway logs --service image-to-codefront
   ```

2. **Проверьте переменные:**
   - Убедитесь, что URL используют `https://` и `wss://`
   - Убедитесь, что `VITE_IS_DEPLOYED=true` (строка)

3. **Перезапустите сервисы:**
   - В Railway Dashboard → Deployments → Redeploy

