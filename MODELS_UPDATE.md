# 🤖 Обновление моделей AI

## ✅ Добавленные модели

### Для генерации кода:

1. **GPT-5** (`gpt-5`)
   - Самая новая модель OpenAI
   - Используется по умолчанию при наличии OpenAI API ключа
   - Fallback: GPT-5 Turbo → GPT-4.1

2. **Claude Opus 4.5** (`claude-opus-4-5-20250929`)
   - Лучшая модель Anthropic для качества кода
   - Используется по умолчанию при наличии Anthropic API ключа
   - Fallback: Claude Sonnet 4.5

3. **Claude Sonnet 4.5** (`claude-sonnet-4-5-20250929`)
   - Быстрая и качественная модель Anthropic
   - Используется как fallback для Opus 4.5

4. **Gemini 3 Pro** (`gemini-3.0-pro`)
   - Новая модель Google для генерации кода
   - Используется как третий вариант при наличии всех трех API ключей

### Для генерации изображений/дизайна:

5. **Gemini 3 Pro Nano** (`gemini-3.0-pro-nano`)
   - Специальная модель для генерации изображений и элементов дизайна
   - Приоритет: Gemini 3 Pro Nano > Flux Schnell > DALL-E 3
   - Используется автоматически при наличии Gemini API ключа

## 🔄 Логика выбора моделей

### Приоритет для генерации кода:

1. **Если доступны все три API ключа (OpenAI + Anthropic + Gemini):**
   - GPT-5
   - Claude Opus 4.5
   - Gemini 3 Pro (для create) или Claude Sonnet 4.5 (для update)

2. **Если доступны OpenAI + Anthropic:**
   - Claude Opus 4.5
   - GPT-5

3. **Если доступен только Anthropic:**
   - Claude Opus 4.5
   - Claude Sonnet 4.5

4. **Если доступен только OpenAI:**
   - GPT-5
   - GPT-5 Turbo (или GPT-4.1 как fallback)

### Приоритет для генерации изображений:

1. **Gemini 3 Pro Nano** (если доступен Gemini API ключ)
2. **Flux Schnell** (если доступен Replicate API ключ)
3. **DALL-E 3** (если доступен OpenAI API ключ)

## 📝 Изменения в коде

### Backend:

- `backend/llm.py`: Добавлены новые модели в enum
- `backend/routes/generate_code.py`: Обновлена логика выбора моделей
- `backend/models/openai_client.py`: Добавлена поддержка GPT-5
- `backend/models/gemini.py`: Добавлена поддержка Gemini 3 Pro
- `backend/image_generation/core.py`: Добавлена поддержка Gemini 3 Pro Nano для генерации изображений

### Frontend:

- `frontend/src/lib/models.ts`: Обновлен список моделей для UI
- `frontend/src/App.tsx`: Дефолтная модель изменена на GPT-5

## ⚠️ Важные замечания:

1. **Gemini 3 Pro Nano для изображений:**
   - Реализация использует placeholder до тех пор, пока официальный API не будет доступен
   - Нужно обновить `generate_image_gemini()` когда API станет доступен

2. **Имена моделей:**
   - Имена моделей могут отличаться в реальном API
   - При необходимости обновите имена в `backend/llm.py`

3. **Fallback логика:**
   - Система автоматически использует fallback модели если основные недоступны
   - Это обеспечивает работу даже если некоторые API ключи отсутствуют

## 🧪 Тестирование:

После деплоя проверьте:
1. Генерация кода работает с новыми моделями
2. Генерация изображений использует Gemini 3 Pro Nano (если доступен ключ)
3. Fallback логика работает корректно

