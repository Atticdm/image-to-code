# 📋 Инструкция по развертыванию и тестированию Image-to-Code

## 🎯 Что нужно для запуска

### Обязательные требования:
1. **Python 3.10+** (для backend)
2. **Node.js 14.18+** (для frontend) ✅ У вас установлен v22.14.0
3. **API ключи** (хотя бы один):
   - OpenAI API ключ с доступом к GPT-4 Vision
   - ИЛИ Anthropic API ключ для Claude

### Опционально:
- Docker (для запуска через Docker Compose)
- Poetry (для управления Python зависимостями)
- Yarn или npm (для управления Node.js зависимостями)

---

## 🚀 Вариант 1: Локальный запуск (рекомендуется для разработки)

### Шаг 1: Установка зависимостей для Backend

```bash
# Установите Poetry (если еще не установлен)
pip install --upgrade poetry

# Перейдите в директорию backend
cd backend

# Установите зависимости
poetry install

# Активируйте виртуальное окружение
poetry shell
```

### Шаг 2: Настройка API ключей для Backend

Создайте файл `.env` в директории `backend/`:

```bash
cd backend
cat > .env << EOF
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=your-anthropic-key-here
EOF
```

**Где получить API ключи:**
- **OpenAI**: https://platform.openai.com/api-keys (нужен доступ к GPT-4 Vision)
- **Anthropic**: https://console.anthropic.com/ (нужен доступ к Claude)

> 💡 **Важно**: Вы можете использовать только один ключ, но рекомендуется иметь оба для сравнения результатов разных моделей.

### Шаг 3: Запуск Backend

```bash
# Убедитесь, что вы в директории backend и в poetry shell
cd backend
poetry run uvicorn main:app --reload --port 7001
```

Backend будет доступен на `http://127.0.0.1:7001`

### Шаг 4: Установка зависимостей для Frontend

Откройте **новый терминал** (backend должен продолжать работать):

```bash
# Установите Yarn (если еще не установлен)
npm install -g yarn

# Перейдите в директорию frontend
cd frontend

# Установите зависимости
yarn install
# или если yarn не работает:
npm install
```

### Шаг 5: Запуск Frontend

```bash
# Убедитесь, что вы в директории frontend
cd frontend
yarn dev
# или если yarn не работает:
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

---

## 🐳 Вариант 2: Запуск через Docker (быстрый старт)

### Шаг 1: Создайте файл .env в корне проекта

```bash
# В корне проекта создайте файл .env
cat > .env << EOF
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=your-anthropic-key-here
BACKEND_PORT=7001
EOF
```

### Шаг 2: Запустите через Docker Compose

```bash
# В корне проекта
docker-compose up -d --build
```

Приложение будет доступно на `http://localhost:5173`

> ⚠️ **Важно**: При использовании Docker изменения в коде не будут автоматически перезагружаться. Это подходит только для тестирования готовой версии.

---

## 🧪 Как протестировать продукт

### Сценарий: Генерация кода из скриншота дизайна сайта

1. **Откройте приложение в браузере**
   - Перейдите на `http://localhost:5173`
   - Вы должны увидеть интерфейс с кнопкой загрузки изображения

2. **Настройте API ключи (если не настроены в .env)**
   - Нажмите на иконку ⚙️ (шестеренка) в правом верхнем углу
   - Введите ваш OpenAI API ключ или Anthropic API ключ
   - Выберите модель (рекомендуется Claude Sonnet 3.7 или GPT-4o)
   - Выберите стек (например, HTML + Tailwind)

3. **Загрузите скриншот дизайна**
   - Нажмите на область загрузки или перетащите изображение
   - Выберите файл со скриншотом вашего дизайна
   - Поддерживаемые форматы: PNG, JPG, JPEG

4. **Дождитесь генерации кода**
   - Система автоматически начнет генерацию
   - Вы увидите прогресс в реальном времени
   - Будет создано несколько вариантов кода (обычно 2-4)

5. **Просмотрите результат**
   - В правой части экрана вы увидите превью сгенерированного кода
   - Вы можете переключаться между вариантами
   - Код отображается в редакторе слева от превью

6. **Редактирование (опционально)**
   - Вы можете ввести текстовые инструкции для изменения кода
   - Например: "Измени цвет кнопки на синий"
   - Система создаст новую версию с изменениями

7. **Экспорт кода**
   - Нажмите на кнопку скачивания для сохранения HTML файла
   - Или скопируйте код из редактора

---

## 🔧 Режим Mock (для тестирования без API ключей)

Если вы хотите протестировать интерфейс без расходования API credits:

```bash
cd backend
MOCK=true poetry run uvicorn main:app --reload --port 7001
```

Это запустит backend с предзаписанными ответами вместо реальных вызовов API.

---

## 🐛 Решение проблем

### Проблема: Backend не запускается

**Решение:**
```bash
# Убедитесь, что Poetry установлен
pip install --upgrade poetry

# Переустановите зависимости
cd backend
poetry install --no-root
poetry shell
poetry run uvicorn main:app --reload --port 7001
```

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Проверьте, что backend запущен на порту 7001
2. Создайте файл `frontend/.env.local`:
```bash
cd frontend
cat > .env.local << EOF
VITE_HTTP_BACKEND_URL=http://127.0.0.1:7001
VITE_WS_BACKEND_URL=ws://127.0.0.1:7001
EOF
```

### Проблема: Ошибка "No API key found"

**Решение:**
- Убедитесь, что файл `backend/.env` существует и содержит правильные ключи
- Или введите ключи через UI (иконка ⚙️ в правом верхнем углу)

### Проблема: Yarn не найден

**Решение:**
```bash
# Установите Yarn глобально
npm install -g yarn

# Или используйте npm вместо yarn
cd frontend
npm install
npm run dev
```

---

## 📝 Быстрая проверка работоспособности

После запуска проверьте:

1. **Backend работает:**
   ```bash
   curl http://127.0.0.1:7001/
   ```
   Должен вернуть JSON ответ

2. **Frontend работает:**
   - Откройте `http://localhost:5173` в браузере
   - Должен загрузиться интерфейс приложения

3. **WebSocket соединение:**
   - Откройте консоль браузера (F12)
   - При загрузке скриншота не должно быть ошибок WebSocket

---

## 🎨 Примеры использования

### Пример 1: Простой дизайн кнопки
1. Создайте скриншот простой кнопки
2. Загрузите в приложение
3. Выберите стек "HTML + Tailwind"
4. Получите готовый код

### Пример 2: Сложная страница
1. Сделайте скриншот страницы сайта
2. Загрузите в приложение
3. Выберите "React + Tailwind" для более сложных компонентов
4. Получите React компонент

### Пример 3: Редактирование
1. После генерации кода введите: "Измени цвет заголовка на красный"
2. Система создаст новую версию с изменениями
3. Вы можете продолжать редактировать итеративно

---

## 📚 Дополнительная информация

- **Документация**: См. `README.md` в корне проекта
- **Тестирование**: См. `TESTING.md` для запуска тестов
- **Устранение неполадок**: См. `Troubleshooting.md`

---

## ✅ Чеклист перед тестированием

- [ ] Python 3.10+ установлен
- [ ] Node.js установлен ✅
- [ ] Poetry установлен (или используйте Docker)
- [ ] Yarn/npm установлен
- [ ] API ключ получен (OpenAI или Anthropic)
- [ ] Backend запущен на порту 7001
- [ ] Frontend запущен на порту 5173
- [ ] Браузер открыт на `http://localhost:5173`
- [ ] API ключ введен в настройках (или в .env файле)

---

**Удачи с тестированием! 🚀**

