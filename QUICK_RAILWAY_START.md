# ⚡ Быстрый старт на Railway

## 🚀 За 5 минут

### 1. Создайте аккаунт на Railway
- Зайдите на https://railway.app
- Войдите через GitHub

### 2. Деплой Backend

1. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `Atticdm/image-to-code`

2. **Добавьте Backend сервис:**
   - Нажмите "New" → "GitHub Repo"
   - Выберите тот же репозиторий
   - В настройках укажите:
     - **Root Directory:** `backend`
     - **Dockerfile Path:** `Dockerfile`

3. **Добавьте переменные окружения:**
   - Settings → Variables → New Variable
   - Добавьте:
     ```
     OPENAI_API_KEY = sk-your-key-here
     ANTHROPIC_API_KEY = your-key-here
     ```
   - Railway автоматически установит `PORT`

4. **Получите URL Backend:**
   - После деплоя перейдите в Settings → Networking
   - Скопируйте URL (например: `https://your-backend.railway.app`)

### 3. Деплой Frontend

1. **Добавьте Frontend сервис:**
   - В том же проекте нажмите "New" → "GitHub Repo"
   - Выберите тот же репозиторий
   - В настройках укажите:
     - **Root Directory:** `frontend`
     - **Dockerfile Path:** `Dockerfile.prod`

2. **Добавьте переменные окружения:**
   - Settings → Variables → New Variable
   - Добавьте (замените на ваш backend URL):
     ```
     VITE_HTTP_BACKEND_URL = https://your-backend.railway.app
     VITE_WS_BACKEND_URL = wss://your-backend.railway.app
     VITE_IS_DEPLOYED = true
     ```

3. **Получите URL Frontend:**
   - После деплоя перейдите в Settings → Networking
   - Скопируйте URL или сгенерируйте домен

### 4. Протестируйте!

1. Откройте URL фронтенда в браузере
2. Загрузите скриншот дизайна
3. Получите код! 🎉

---

## 📝 Важные моменты

- **WebSocket:** Используйте `wss://` (не `ws://`) для WebSocket соединения
- **CORS:** Backend уже настроен на разрешение всех источников
- **Порты:** Railway автоматически назначает порты, используйте переменную `PORT`

---

## 🔧 Если что-то не работает

1. **Проверьте логи:**
   - В Railway перейдите в сервис → Logs
   - Ищите ошибки

2. **Проверьте переменные окружения:**
   - Убедитесь, что все переменные установлены правильно
   - Особенно `VITE_WS_BACKEND_URL` должен начинаться с `wss://`

3. **Проверьте URL:**
   - Backend должен быть доступен публично
   - Frontend должен иметь правильные URL для backend

---

## 💡 Советы

- Railway автоматически деплоит при каждом пуше в main ветку
- Используйте Preview Deployments для тестирования перед продакшеном
- Мониторьте использование ресурсов в Dashboard

**Готово! 🚂**

