# 🚂 Деплой на Railway

## 📋 Подготовка

### 1. Установите Railway CLI (опционально)

```bash
npm install -g @railway/cli
railway login
```

Или используйте веб-интерфейс: https://railway.app

### 2. Подготовьте API ключи

Вам понадобятся:
- `OPENAI_API_KEY` - ключ OpenAI (опционально, но рекомендуется)
- `ANTHROPIC_API_KEY` - ключ Anthropic (опционально, но рекомендуется)
- `GEMINI_API_KEY` - ключ Google Gemini (опционально)
- `REPLICATE_API_KEY` - для генерации изображений (опционально)

---

## 🎯 Вариант 1: Деплой через Railway CLI

### Backend

```bash
# Перейдите в директорию backend
cd backend

# Инициализируйте Railway проект
railway init

# Создайте новый проект или выберите существующий
railway link

# Установите переменные окружения
railway variables set OPENAI_API_KEY=sk-your-key
railway variables set ANTHROPIC_API_KEY=your-key

# Деплой
railway up
```

### Frontend

```bash
# Перейдите в директорию frontend
cd frontend

# Инициализируйте Railway проект
railway init

# Свяжите с проектом (или создайте новый)
railway link

# Установите переменные окружения для подключения к backend
railway variables set VITE_HTTP_BACKEND_URL=https://your-backend-url.railway.app
railway variables set VITE_WS_BACKEND_URL=wss://your-backend-url.railway.app
railway variables set VITE_IS_DEPLOYED=true

# Деплой
railway up
```

---

## 🌐 Вариант 2: Деплой через веб-интерфейс Railway

### Шаг 1: Создайте проект

1. Зайдите на https://railway.app
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo" или "Empty Project"

### Шаг 2: Деплой Backend

1. **Добавьте сервис Backend:**
   - Нажмите "New" → "GitHub Repo"
   - Выберите ваш репозиторий
   - Выберите директорию `backend`

2. **Настройте переменные окружения:**
   - Перейдите в Settings → Variables
   - Добавьте:
     ```
     OPENAI_API_KEY=sk-your-key
     ANTHROPIC_API_KEY=your-key
     PORT=7001
     ```

3. **Настройте деплой:**
   - Railway автоматически обнаружит Dockerfile
   - Или укажите в Settings → Deploy:
     - Root Directory: `backend`
     - Dockerfile Path: `Dockerfile`

4. **Получите URL:**
   - После деплоя Railway предоставит URL вида: `https://your-backend.railway.app`
   - Сохраните этот URL для настройки фронтенда

### Шаг 3: Деплой Frontend

1. **Добавьте сервис Frontend:**
   - Нажмите "New" → "GitHub Repo"
   - Выберите тот же репозиторий
   - Выберите директорию `frontend`

2. **Настройте переменные окружения:**
   - Перейдите в Settings → Variables
   - Добавьте:
     ```
     VITE_HTTP_BACKEND_URL=https://your-backend.railway.app
     VITE_WS_BACKEND_URL=wss://your-backend.railway.app
     VITE_IS_DEPLOYED=true
     PORT=80
     ```

3. **Настройте деплой:**
   - Root Directory: `frontend`
   - Dockerfile Path: `Dockerfile.prod`
   - Build Command: `yarn build`
   - Start Command: `nginx -g "daemon off;"`

4. **Настройте домен (опционально):**
   - Settings → Generate Domain
   - Или подключите свой домен

---

## 🔧 Конфигурация для Production

### Backend переменные окружения:

```env
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=... (опционально)
REPLICATE_API_KEY=... (опционально)
PORT=7001
IS_PROD=true
```

### Frontend переменные окружения:

```env
VITE_HTTP_BACKEND_URL=https://your-backend.railway.app
VITE_WS_BACKEND_URL=wss://your-backend.railway.app
VITE_IS_DEPLOYED=true
```

---

## 🔗 Связывание сервисов

Railway автоматически создает внутреннюю сеть между сервисами. Вы можете использовать:

1. **Внутренний URL** (для связи между сервисами):
   - Backend: `http://backend:7001`
   - Но для фронтенда лучше использовать публичный URL

2. **Публичный URL** (для фронтенда):
   - Получите URL из Settings → Networking
   - Используйте его в переменных окружения фронтенда

---

## 📝 Проверка деплоя

### Backend:

```bash
# Проверьте здоровье сервиса
curl https://your-backend.railway.app/

# Должен вернуть JSON ответ
```

### Frontend:

1. Откройте URL фронтенда в браузере
2. Проверьте консоль браузера (F12) на ошибки
3. Попробуйте загрузить скриншот и сгенерировать код

---

## 🐛 Решение проблем

### Проблема: Backend не запускается

**Решение:**
1. Проверьте логи: `railway logs` или в веб-интерфейсе
2. Убедитесь, что переменные окружения установлены
3. Проверьте, что PORT используется правильно

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Убедитесь, что `VITE_HTTP_BACKEND_URL` и `VITE_WS_BACKEND_URL` правильные
2. Проверьте, что backend доступен публично
3. Убедитесь, что используется `wss://` для WebSocket (не `ws://`)

### Проблема: WebSocket соединение не работает

**Решение:**
1. Railway автоматически проксирует WebSocket
2. Убедитесь, что используете `wss://` (безопасное соединение)
3. Проверьте CORS настройки в backend

### Проблема: Изображения не загружаются

**Решение:**
1. Проверьте размер файлов (Railway имеет лимиты)
2. Убедитесь, что CORS настроен правильно
3. Проверьте логи backend на ошибки

---

## 💰 Стоимость

Railway предоставляет:
- **Free tier**: $5 кредитов в месяц
- **Pro**: $20/месяц за дополнительные ресурсы

Для этого проекта достаточно Free tier для тестирования.

---

## 🔄 Автоматический деплой

Railway автоматически деплоит при пуше в основную ветку (main/master), если вы подключили GitHub репозиторий.

Для настройки:
1. Settings → Source
2. Выберите ветку для автодеплоя
3. Включите "Auto Deploy"

---

## 📚 Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [Примеры деплоя](https://docs.railway.app/examples)

---

## ✅ Чеклист деплоя

- [ ] Railway аккаунт создан
- [ ] GitHub репозиторий подключен
- [ ] Backend сервис создан
- [ ] Backend переменные окружения установлены
- [ ] Backend URL получен
- [ ] Frontend сервис создан
- [ ] Frontend переменные окружения установлены (с backend URL)
- [ ] Оба сервиса успешно задеплоены
- [ ] Домен настроен (опционально)
- [ ] Приложение протестировано

**Готово! 🎉**

