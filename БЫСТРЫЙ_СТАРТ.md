# ⚡ Быстрый старт - Image-to-Code

## 🎯 Минимальные требования (у вас уже есть!)

✅ Python 3.13.7  
✅ Node.js v22.14.0  
✅ npm 10.9.2

---

## 🚀 Шаг за шагом (5 минут)

### 1️⃣ Установите Poetry для Python

```bash
pip install --upgrade poetry
```

### 2️⃣ Настройте Backend

```bash
# Перейдите в директорию backend
cd backend

# Установите зависимости
poetry install

# Создайте файл .env с API ключом
echo "OPENAI_API_KEY=sk-your-key-here" > .env
# ИЛИ
echo "ANTHROPIC_API_KEY=your-key-here" > .env

# Запустите backend (в отдельном терминале)
poetry run uvicorn main:app --reload --port 7001
```

> 💡 **Где взять API ключ?**
> - OpenAI: https://platform.openai.com/api-keys
> - Anthropic: https://console.anthropic.com/

### 3️⃣ Настройте Frontend

Откройте **новый терминал**:

```bash
# Перейдите в директорию frontend
cd frontend

# Установите зависимости (используйте npm, так как yarn не установлен)
npm install

# Запустите frontend
npm run dev
```

### 4️⃣ Откройте приложение

Откройте браузер: **http://localhost:5173**

### 5️⃣ Протестируйте!

1. Нажмите на иконку ⚙️ (настройки) в правом верхнем углу
2. Введите ваш API ключ (если не добавили в .env)
3. Выберите модель и стек (например, "HTML + Tailwind")
4. Загрузите скриншот дизайна
5. Дождитесь генерации кода
6. Посмотрите результат в превью справа!

---

## 🧪 Тестирование без API ключей (Mock режим)

Если хотите протестировать интерфейс без расходования credits:

```bash
cd backend
MOCK=true poetry run uvicorn main:app --reload --port 7001
```

---

## ❓ Проблемы?

### Backend не запускается?
```bash
cd backend
poetry install --no-root
poetry shell
poetry run uvicorn main:app --reload --port 7001
```

### Frontend не подключается?
Убедитесь, что backend запущен на порту 7001 и доступен по адресу `http://127.0.0.1:7001`

### Нужна помощь?
См. подробную инструкцию: `ИНСТРУКЦИЯ_ПО_РАЗВЕРТЫВАНИЮ.md`

---

## 📋 Чеклист

- [ ] Poetry установлен (`pip install --upgrade poetry`)
- [ ] Backend зависимости установлены (`cd backend && poetry install`)
- [ ] API ключ добавлен в `backend/.env`
- [ ] Backend запущен (`poetry run uvicorn main:app --reload --port 7001`)
- [ ] Frontend зависимости установлены (`cd frontend && npm install`)
- [ ] Frontend запущен (`npm run dev`)
- [ ] Браузер открыт на `http://localhost:5173`

**Готово! 🎉**

