# 🔗 Настройка GitHub Auto-Deploy на Railway

## Проблема

Railway сейчас делает деплой через CLI (`railway up`), поэтому не подхватывает автоматически пуш из GitHub.

## ✅ Решение: Подключить GitHub через Dashboard

### Пошаговая инструкция:

1. **Откройте Railway Dashboard:**
   - Перейдите: https://railway.app/project/ff479365-a5ed-45ff-9afc-e38d711e7fbc
   - Или: https://railway.app → выберите проект "Image-to-code"

2. **Выберите сервис:**
   - Кликните на сервис `image-to-code`

3. **Настройте Source (Источник):**
   - Перейдите в **Settings** (шестеренка в правом верхнем углу)
   - Или кликните на вкладку **Settings** в меню
   - Найдите секцию **"Source"** или **"Repository"**

4. **Подключите GitHub:**
   - Если видите **"Connect GitHub"** или **"Change Source"** — нажмите
   - Выберите репозиторий: `Atticdm/image-to-code`
   - Выберите branch: `main`
   - **ВАЖНО:** Включите переключатель **"Auto Deploy"** ✅
   - Сохраните изменения

5. **Проверьте настройки:**
   - В секции Source должно быть указано:
     - **Repository:** `Atticdm/image-to-code`
     - **Branch:** `main`
     - **Auto Deploy:** `Enabled` ✅

### Что произойдет:

- Railway автоматически создаст webhook в вашем GitHub репозитории
- При каждом пуше в `main` будет запускаться новый деплой
- В логах деплоя будет указано: `via GitHub` вместо `via CLI`
- Вы увидите коммит-сообщение и автора в истории деплоев

### Проверка работы:

1. Сделайте тестовый коммит:
   ```bash
   git commit --allow-empty -m "Test GitHub auto-deploy"
   git push origin main
   ```

2. В Railway Dashboard → Deployments:
   - Должен появиться новый деплой
   - В описании будет: `via GitHub`
   - Будет видно коммит-сообщение

## 🔧 Если GitHub уже подключен, но Auto Deploy выключен:

1. Откройте Settings → Source
2. Найдите переключатель **"Auto Deploy"**
3. Включите его ✅
4. Сохраните

## 📝 Альтернатива: Через Railway CLI (если Dashboard недоступен)

Railway CLI не поддерживает прямое подключение GitHub репозитория. Это нужно делать через Dashboard.

Однако можно проверить текущий статус:
```bash
railway status
```

## ⚠️ Важные моменты:

1. **Root Directory:** Убедитесь, что в Settings → Source установлен **Root Directory: `backend`**
2. **Webhook:** Railway автоматически создаст webhook в GitHub после подключения
3. **Права доступа:** Railway должен иметь доступ к вашему GitHub репозиторию

## 🎯 Текущие настройки проекта:

- **Project ID:** `ff479365-a5ed-45ff-9afc-e38d711e7fbc`
- **Service:** `image-to-code`
- **Repository:** `Atticdm/image-to-code` (нужно подключить)
- **Branch:** `main`
- **Root Directory:** `backend` (должно быть установлено)

После настройки GitHub integration все будущие пуши будут автоматически деплоиться! 🚀

