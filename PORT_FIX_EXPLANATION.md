# 🔧 Объяснение проблемы с PORT

## Проблема

В логах Railway видно ошибку:
```
Error: Invalid value for '--port': '$PORT' is not a valid integer.
```

## Причина

Railway использует `startCommand` из `railway.toml`, но переменная `$PORT` не подставляется, потому что команда выполняется напрямую без shell.

## Решение

Использовать shell wrapper для подстановки переменной:

**Было (не работает):**
```toml
startCommand = "poetry run uvicorn main:app --host 0.0.0.0 --port $PORT"
```

**Стало (работает):**
```toml
startCommand = "sh -c 'poetry run uvicorn main:app --host 0.0.0.0 --port ${PORT:-7001}'"
```

## Что изменилось

1. ✅ `backend/railway.toml` - обновлен startCommand с shell wrapper
2. ✅ `backend/Dockerfile` - обновлен CMD с shell wrapper
3. ✅ `railway.toml` (корневой) - обновлен startCommand

## Проверка

После деплоя коммита `9517ba7` Railway должен:
1. Прочитать обновленный `backend/railway.toml`
2. Использовать новый `startCommand` с shell wrapper
3. Правильно подставить переменную PORT

## Если проблема осталась

1. Проверьте, что Railway задеплоил коммит `9517ba7`
2. Проверьте логи деплоя - должна быть команда с `sh -c`
3. Убедитесь, что Railway не переопределяет startCommand в Settings

